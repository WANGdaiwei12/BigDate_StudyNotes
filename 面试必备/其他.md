##### 什么是压缩，什么是序列化

```tex
电脑里文件都是以二进制储存的。压缩原理就是通过特定的算法，将文件转化，而转化以后的文件占用的空间较小。
或者也可以理解通过找规律，简化这些**0**和**1**之间的排列组合。
有哪些算法
RLE（Run Length Encoding）算法：行程长度编码
哈夫曼编码

序列化：把对象转换为字节序列的过程称为对象的序列化；
反序列化：把字节序列恢复为对象的过程称为对象的反序列化。
用处：1） 把对象的字节序列永久地保存到硬盘上，通常存放在一个文件中；
	 2） 在网络上传送对象的字节序列。
```

